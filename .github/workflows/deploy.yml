name: Validate Deployment Setup

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deployment-note:
    runs-on: ubuntu-latest

    steps:
      - name: Explain deployment flow
        run: |
          echo "Production deployment is handled by the cPanel cron job."
          echo "This workflow exists as a reminder that pushes to main are picked up from GitHub by cPanel."
